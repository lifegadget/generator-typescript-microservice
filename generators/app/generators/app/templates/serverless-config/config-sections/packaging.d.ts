import { IServerlessPackage } from 'common-types';
import { IServerlessAccountInfo } from './types';
export declare const packaging: (config: IServerlessAccountInfo) => {
    package: IServerlessPackage;
};
