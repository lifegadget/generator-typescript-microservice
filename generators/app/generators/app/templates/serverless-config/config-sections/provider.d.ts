import { IServerlessProvider } from 'common-types';
import { IServerlessAccountInfo } from './types';
export declare const provider: (config: IServerlessAccountInfo) => {
    provider: IServerlessProvider;
};
